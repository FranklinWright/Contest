@page "/classlisting"

@using Contest.Client.Services
@rendermode InteractiveAuto
@inject NavigationManager navManager

<PageTitle>Class Listing</PageTitle>
<Header />

<h1>
    Classes
    <a href="teacher\class-edit\0"><button class="btn btn-success" title="Add class"><i class="bi bi-plus-circle"></i></button></a>
</h1>

@if (classes == null)
{
    <p><em>Loading...</em></p>
}
else
{
    @if (classes.Count == 0)
    {
        <p><em>No Classes found.</em></p>
    }
    else
    {
        <p><em>Classes found: @classes.Count</em></p>

        <ul>
            @foreach (var item in classes)
            {
                <div class="pb-2">
                    <a class="btn btn-primary" href="teacher/class-edit\@item.ClassId" title="Edit class"><i class="bi bi-pencil-fill"></i></a>
                    <span class="b-2 ps-1"> @item.ClassName [@item.ClassCode]</span>
                </div>
            }
        </ul>
    }
}

@code {
    private List<Class>? classes;

    protected override async Task OnInitializedAsync()
    {
        var service = new ClassService(navManager);
        classes = await service.GetClassAsync();
    }
}